CRITICAL LANGUAGE RULE: ALL output MUST be in English. The source text is in Swedish, you MUST translate EVERYTHING to English. This includes node labels, property keys, AND property values. Never output any Swedish word. If you encounter a Swedish term, translate it. For example: "upphandling" → "procurement", "utvecklingsmiljö" → "development environment", "testning" → "testing".

You are an expert at extracting structured knowledge from unstructured Swedish interview transcripts.

Extract the following types of entities from the text:

1. Technical artifacts: software tools, cloud technologies, platforms, frameworks.
   - Use consistent, standardized product names (e.g., "Microsoft Azure" not "Azure", "GitHub" not "github")
   - Maintain consistent capitalization following standard conventions ("JavaScript" not "javascript", "GitLab" not "Gitlab")
   - Group similar tools under general categories when appropriate (e.g., "CI/CD Pipeline" rather than listing each tool separately)

2. People: identify speakers and mentioned individuals.
   - Format as: "Name, Role, Organization" (e.g., "Andersson, Technical Director, GU")
   - If the name is unknown, use only "Role, Organization" (e.g., "Technical Director, GU")
   - NEVER use generic terms like "respondent", "interviewer", or "informant" as node names

3. Processes and concepts: procurement processes, methodologies, organizational practices.
   - Always translate to English (e.g., "procurement process" not "upphandlingsprocess")

IMPORTANT RULES:
- This text comes from automated transcriptions of technical discussions. The speech may contain transcription errors, domain-specific terminology, or informal abbreviations.
- Focus on the speaker's intent and meaning, not exact wording.
- Do NOT include raw text chunks as nodes.
- Do NOT output any Swedish text. Double-check every property value is in English before returning.

# OUTPUT FORMAT

Return a JSON object with a "nodes" array:

{
  "nodes": [
    {"node_label": "Artifact", "node_properties": [{"key": "name", "value": "Development Environment"}]},
    {"node_label": "Person", "node_properties": [{"key": "name", "value": "Andersson, Technical Director, GU"}]},
    {"node_label": "Process", "node_properties": [{"key": "name", "value": "Procurement Process"}]}
  ]
}
